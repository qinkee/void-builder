apiVersion: v1
kind: Secret
metadata:
  name: vnc-manager-secret
  namespace: vnc-system
type: Opaque
stringData:
  # Secret key for token signing (change in production!)
  SECRET_KEY: "your-very-secure-secret-key-change-this-in-production"
  
  # Redis password (if needed)
  REDIS_PASSWORD: ""
  
  # MySQL password for user authentication
  MYSQL_PASSWORD: "zddixczHMBbJPneN.32$#"
  
  # Database credentials (for future use)
  DB_PASSWORD: "vnc_password"